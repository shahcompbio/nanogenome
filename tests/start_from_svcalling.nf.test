nextflow_pipeline {

    name "Test pipeline"
    script "../main.nf"
    tag "pipeline"

    test("-profile sv_only") {

        when {
            params {
                outdir = "$outputDir"
                germline = true
                skip_phasing = true
                input = 'https://raw.githubusercontent.com/apsteinberg/test-datasets/nanogenome/samplesheet_sv_only.csv'

            }
        }

        then {
            assert workflow.success
        }
    }
}
