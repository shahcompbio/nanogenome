nextflow_pipeline {

    name "Test somatic sv calling only workflow"
    script "../main.nf"
    tag "pipeline"

    test("somatic sv calling only workflow") {
        options "-stub"

        when {
            params {
                outdir = "$outputDir"
                germline = false
                skip_phasing = true
                skip_cna = true
                input = 'https://raw.githubusercontent.com/apsteinberg/test-datasets/nanogenome/samplesheet_sv_only.csv'

            }
        }

        then {
            assert workflow.success
        }
    }
}
